# ============================================================================
# 通用反向代理转发头（被 nginx.conf 各 location 以 include 复用）
#
# 注意：nginx 的 proxy_set_header 是「按块覆盖」的——只要某个 location 内
# 出现任意 proxy_set_header，就不再继承 server 级的同类指令。因此把转发头
# 集中到本文件并在每个 location 内 include，确保 Host / X-Forwarded-* 始终生效。
# ============================================================================
proxy_set_header Host              $host;
proxy_set_header X-Real-IP         $remote_addr;
proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
proxy_set_header X-Forwarded-Proto $scheme;
proxy_set_header X-Forwarded-Host  $host;
